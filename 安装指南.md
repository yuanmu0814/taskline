# Node.js 安装指南

## 方法一：使用官方安装包（推荐）

1. **下载 Node.js**
   - 访问 Node.js 官网：https://nodejs.org/zh-cn/
   - 下载 LTS（长期支持）版本（推荐）
   - 选择 Windows Installer (.msi) 64-bit

2. **安装 Node.js**
   - 运行下载的 .msi 安装包
   - 按照安装向导完成安装（保持默认选项即可）
   - 安装程序会自动将 Node.js 和 npm 添加到系统 PATH

3. **验证安装**
   - 关闭并重新打开 PowerShell 或命令提示符
   - 运行以下命令验证：
   ```powershell
   node --version
   npm --version
   ```

## 方法二：使用包管理器（如果已安装）

### 使用 Chocolatey（如果已安装）
```powershell
choco install nodejs
```

### 使用 winget（Windows 10/11 自带）
```powershell
winget install OpenJS.NodeJS.LTS
```

## 安装完成后

1. **重新打开终端**（重要！）
   - 关闭当前的 PowerShell 窗口
   - 重新打开一个新的 PowerShell 窗口

2. **验证安装**
   ```powershell
   node --version
   npm --version
   ```

3. **安装项目依赖**
   ```powershell
   cd D:\taskline
   npm install
   ```

4. **启动开发服务器**
   ```powershell
   npm run dev
   ```

## 常见问题

### 问题：安装后仍然提示找不到命令
**解决方案**：
1. 确保在安装时勾选了"Add to PATH"选项
2. 重启计算机
3. 手动检查环境变量：
   - 打开"系统属性" > "高级" > "环境变量"
   - 在"系统变量"的 Path 中应该包含 Node.js 的安装路径（通常是 `C:\Program Files\nodejs\`）

### 问题：权限问题
**解决方案**：
- 以管理员身份运行 PowerShell
- 或者使用用户级别的安装（不需要管理员权限）

## 快速检查命令

安装完成后，运行以下命令检查环境：

```powershell
# 检查 Node.js 版本
node --version

# 检查 npm 版本
npm --version

# 检查 Node.js 安装路径
where.exe node

# 检查 npm 安装路径
where.exe npm
```


