# 服务器问题排查指南

## 错误：ENOENT: no such file or directory, uv_cwd

这个错误表示当前工作目录不存在或已被删除。

### 解决方案

#### 1. 检查并切换到正确的目录

```bash
# 检查当前目录是否存在
pwd

# 如果目录不存在，切换到项目目录
cd /path/to/taskline

# 或者重新创建目录并进入
mkdir -p /path/to/taskline
cd /path/to/taskline
```

#### 2. 如果使用 PM2，确保在正确的目录

```bash
# 停止 PM2 进程
pm2 stop all
pm2 delete all

# 进入项目目录
cd /path/to/taskline

# 重新启动
cd server
pm2 start index.js --name taskline-server
```

#### 3. 检查目录权限

```bash
# 确保有执行权限
chmod +x /path/to/taskline
chmod +x /path/to/taskline/server
```

#### 4. 使用绝对路径启动（推荐）

创建启动脚本 `start-server.sh`:

```bash
#!/bin/bash
cd /absolute/path/to/taskline/server
node index.js
```

然后：
```bash
chmod +x start-server.sh
./start-server.sh
```

#### 5. 使用 PM2 的 cwd 选项

```bash
pm2 start index.js --name taskline-server --cwd /absolute/path/to/taskline/server
```

### 快速修复步骤

1. **找到项目目录**:
```bash
find / -name "taskline" -type d 2>/dev/null
```

2. **进入项目目录**:
```bash
cd /找到的路径/taskline
```

3. **验证目录结构**:
```bash
ls -la
# 应该看到 server/, src/, package.json 等
```

4. **重新启动服务**:
```bash
cd server
npm install  # 如果需要
npm start
```

### 预防措施

1. **使用绝对路径**：在启动脚本中使用绝对路径
2. **使用 PM2 ecosystem 文件**：配置工作目录
3. **检查目录存在性**：在脚本中添加检查



