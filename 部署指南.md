# Linux 服务器部署指南

## 方法一：开发模式部署（临时测试）

### 1. 配置 Vite 服务器

已配置 `vite.config.ts` 监听所有网络接口（0.0.0.0），可以直接使用：

```bash
npm run dev
```

### 2. 访问应用

- 服务器内网访问：`http://服务器IP:5173`
- 外网访问：`http://服务器公网IP:5173`

### 3. 防火墙配置

如果无法访问，需要开放端口：

```bash
# Ubuntu/Debian
sudo ufw allow 5173/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-port=5173/tcp
sudo firewall-cmd --reload
```

### 4. 使用 PM2 保持运行（推荐）

```bash
# 安装 PM2
npm install -g pm2

# 使用 PM2 运行开发服务器
pm2 start npm --name "taskline-dev" -- run dev

# 查看状态
pm2 status

# 查看日志
pm2 logs taskline-dev

# 停止
pm2 stop taskline-dev
```

---

## 方法二：生产模式部署（推荐）

### 1. 构建生产版本

```bash
npm run build
```

构建完成后，会在 `dist` 目录生成生产文件。

### 2. 使用 Nginx 部署（推荐）

#### 安装 Nginx

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nginx

# CentOS/RHEL
sudo yum install nginx
```

#### 配置 Nginx

创建配置文件 `/etc/nginx/sites-available/taskline`（Ubuntu/Debian）或 `/etc/nginx/conf.d/taskline.conf`（CentOS）：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名或IP

    root /path/to/taskline/dist;  # 替换为你的项目路径
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

#### 启用配置

```bash
# Ubuntu/Debian
sudo ln -s /etc/nginx/sites-available/taskline /etc/nginx/sites-enabled/
sudo nginx -t  # 测试配置
sudo systemctl reload nginx

# CentOS/RHEL
sudo nginx -t
sudo systemctl reload nginx
```

#### 开放 80 端口

```bash
# Ubuntu/Debian
sudo ufw allow 80/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --reload
```

### 3. 使用 Node.js 服务器部署

#### 安装 serve

```bash
npm install -g serve
```

#### 运行生产服务器

```bash
# 在项目目录下
npm run build
serve -s dist -l 3000
```

#### 使用 PM2 管理

```bash
# 安装 PM2
npm install -g pm2

# 使用 PM2 运行
pm2 serve dist 3000 --name "taskline"

# 查看状态
pm2 status

# 设置开机自启
pm2 startup
pm2 save
```

---

## 方法三：使用 Docker 部署（可选）

### 创建 Dockerfile

```dockerfile
# 构建阶段
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# 运行阶段
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### 创建 nginx.conf

```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### 构建和运行

```bash
docker build -t taskline .
docker run -d -p 80:80 --name taskline-app taskline
```

---

## 快速部署脚本

创建 `deploy.sh`：

```bash
#!/bin/bash

# 安装依赖
npm install

# 构建生产版本
npm run build

# 如果使用 PM2
pm2 stop taskline 2>/dev/null || true
pm2 delete taskline 2>/dev/null || true
pm2 serve dist 3000 --name "taskline" --spa
pm2 save

echo "部署完成！访问 http://服务器IP:3000"
```

使用：

```bash
chmod +x deploy.sh
./deploy.sh
```

---

## 常见问题

### 1. 端口被占用

修改 `vite.config.ts` 中的端口号，或使用环境变量：

```bash
PORT=3000 npm run dev
```

### 2. 外网无法访问

- 检查防火墙设置
- 检查云服务器安全组规则
- 确认服务器监听 0.0.0.0 而不是 127.0.0.1

### 3. 生产环境路由问题

确保 Nginx 配置了 `try_files $uri $uri/ /index.html;`，这样前端路由才能正常工作。

---

## 推荐方案

**生产环境推荐使用 Nginx + 生产构建版本**，性能更好，更稳定。

**开发/测试环境可以使用 PM2 + 开发服务器**，方便调试。

